<?php

/* Utilities for dealing with *Unordered Lists*. An *Unordered List* is defined
 * as a PHP array of size _n_ containing only the integer keys 0 through
 * _n_ - 1, but whose ordering of these elements is ignored; only the mapping
 * of indexes to values is significant. Every *UnorderedList* is a valid
 * *Map*. */
class UnorderedListUtil extends MapUtil {

	// TODO
}

?>
