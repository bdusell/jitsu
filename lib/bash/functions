if [[ -z $_INCLUDED_FUNCTIONS_ ]]; then
_INCLUDED_FUNCTIONS_=1

confirm() {
  local LINE
  until [[ $LINE =~ ^(y(es)?|no?)$ ]]; do
    echo -n "$1 [yn] "
    read LINE
  done
  [[ $LINE =~ y ]]
}

fi
